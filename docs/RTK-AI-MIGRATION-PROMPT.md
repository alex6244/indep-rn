# Промпт: миграция AI-модуля на Redux Toolkit + RTK Query

Скопируйте блок ниже в новый чат Cursor (Agent) с открытым репозиторием `indep-rn`.

---

## Текст промпта

```txt
 
```

---

## Короткая версия

```txt
Мигрируй `src/features/aiPicker` на Redux Toolkit + RTK Query: вынеси `meta/chat/leads` в `aiPickerApi.ts`, подключи store/provider, переведи `AiPickerScreen` на RTK hooks, сохрани текущий fallback и server-error поведение. Без DeepSeek, без рефакторинга всего проекта. Запусти typecheck/tests и дай отчёт по файлам.
```
